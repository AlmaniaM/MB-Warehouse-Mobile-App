import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MB-Warehouse-Mobile-App',
  webDir: 'www'
};

export default config;
